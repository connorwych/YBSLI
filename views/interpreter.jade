extends template/template.jade

block pageMeta
   title | Yorkshire BSL Interpreters 
   meta(name='description' content= "Portal for contacting freelance BSL/English interpreters in the Yorkshire region")

block prepend navigation
  - var navTitle          = "Interpreters";

block append scripts
  script(type='text/javascript', src='/js/sendemail.js')
  script(type='text/javascript', src='/js/findinterpeter.js')
  script(src='https://www.google.com/recaptcha/api.js')
  script(src='/js/boostrapvalidator.js')
  script(src='/js/validate.js')



//block append styles
//  link(rel='stylesheet', href='css/interpreter-grid.css')
//      form(action="/services/sendGroupEmail" method="post")

block lcontent
    #interpreterEntryContainer.row
    hline
    .contact
      form( action="/services/sendGroupEmail" method="post" id="contactForm")
        input(type="hidden" id="interpreterID" name="interpreterID")
        .form-inline
          h3 Send message to:
        .form-horizontal
          #nameGroup.form-group
            label.control-label.col-sm-3.ybsli-control-label(for='name') Your name
            .col-sm-9
              input#name.form-control(type='text' name="senderName" placeholder="Your name")
          #emailGroup.form-group
            label.control-label.col-sm-3.ybsli-control-label(for='email') Your email
            .col-sm-9
              input#email.form-control(type='text' name="senderEmail" placeholder="Your email")
        div
          h3 Your message
          .form-group
            label.control-label(for="Subject") Subject
            input(type="text" class="form-control" id="subject" name="subject" placeholder="Subject")
          .form-group
            label.control-label(for="emailBody") Message
            textarea(class="form-control" id="emailBody" name="emailBody" rows="3")
          .form-group
            #g-recaptcha.g-recaptcha(data-sitekey="6LflT_ASAAAAAKMxtaXIdissZbPBHEnPmj7giH4k" data-callback="gCallback")
            input(type='hidden' id="recaptchaHidden" name="recaptchaHidden")
    
        button(type="submit" id="emailSubmit" class="btn btn-default") Submit
        
block rcontent
    h2  About 
    p This website is a quick and convenient way for you to contact qualified, registered British Sign Language interpreters who live in the Yorkshire region.
    p A simple one click button lets you send an email to all the interpreters listed saving you time and money.
